package org.briljantframework.learning.linear;

import org.junit.Test;

public class LinearRegressionTest {

    @Test
    public void testFit() throws Exception {
//        Container<Frame, Series> container = new CSVInputStream(new FileInputStream("erlang/test.txt")).read(Frame
//                .FACTORY, Series.FACTORY);
//        container = Transformers.removeIncompleteCases(container);
//
//        LinearRegression lr = LinearRegression.create();
//        LinearRegression.Model lrModel = lr.fit(container);
//        int index = 49;
//        for (int i = 0; i < container.rows(); i++) {
//            System.out.println(i + " == " + lrModel.predict(container.getDataset().getEntry(i)) + " " + container
//                    .getTarget().get(i));
//        }

    }
}
package org.briljantframework.dataframe.transform;

import org.junit.Test;

public class RemoveIncompleteColumnsTest {

  @Test
  public void testFit() throws Exception {
    // Container<DenseDataset, DefaultTarget> container = new CSVInputStream(new
    // FileInputStream("erlang/test.txt"))
    // .read(DenseDataset.FACTORY, DefaultTarget.FACTORY);
    //
    // Transformer<Dataset, Target> transformer = new RemoveIncompleteColumns();
    // container = transformer.fitTransform(container);
    // System.out.println(container.getHeaders());
    // assertEquals(150, container.getDataset().rows());
    // assertEquals(4, container.columns());
  }
}

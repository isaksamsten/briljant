package org.briljantframework.data.transform;

import org.junit.Test;

public class RemoveIncompleteCasesTest {

    @Test
    public void testTransform() throws Exception {
//        Container<DenseDataset, DefaultTarget> container = new CSVInputStream(new FileInputStream("erlang/test.txt"))
//                .read(DenseDataset.FACTORY, DefaultTarget.FACTORY);
//        Transformer<Dataset, Target> transformer = new RemoveIncompleteCases();
//        container = transformer.fitTransform(container);
//        assertEquals(146, container.getDataset().rows());
    }
}